This is a project [xaust gx-oa system]
based Struts2 + Spring + Mybatis and
also using the other framework of JQuery, Bootstrap etc.

To build the application's war file run mvn clean package
from the project's root folder.

The war file is created in the target sub-folder.

Copy the war file to your Servlet container (e.g. Tomcat, GlassFish) and 
then startup the Servlet container.

TODO List:
 1.字体，模仿Spring官网 【font-family: "Varela Round",sans-serif;】
 2.footer随着表示内容在底部固定（内容不够一屏固定底部，内容超过一屏固定在内容最后）
